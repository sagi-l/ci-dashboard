apiVersion: v1
kind: Service
metadata:
  name: ci-dashboard-dev
  namespace: ci-dashboard-dev
spec:
  type: ClusterIP
  selector:
    app: ci-dashboard-dev
  ports:
    - name: http
      port: 80
      targetPort: 5000
