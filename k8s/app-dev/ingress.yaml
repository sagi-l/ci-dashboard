apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ci-dashboard-dev-ingress
  namespace: ci-dashboard-dev
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
  - host: dev.cicd-lab.dev
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ci-dashboard-dev
            port:
              number: 80
